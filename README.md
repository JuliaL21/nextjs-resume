# Julia Thron Lobo — Resume (Next.js)

A single-page resume website built with Next.js (App Router) and styled entirely with Tailwind CSS utility classes.

## Run Locally
```bash
npm install
npm run dev
